/**
 * @typedef {Object} User
 * @property {number} id - User's ID
 * @property {string} username - User's username
 * @property {string} email - User's email
 * @property {string} createdAt - User's account creation timestamp
 * @property {string} updatedAt - User's last updated timestamp
 * @property {string} [profilePicUrl] - Optional profile picture URL
 * @property {string[]} roles - Array of user roles (e.g., ['USER', 'ADMIN'])
 */

/** 
 * @type {User} 
 */
export const User = {};
